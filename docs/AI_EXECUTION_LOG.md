## Iteration 1: Initial Implementation Plan & Build

| Task | AI Recommendation | Engineer Decision | Status | Rationale | Validation Performed |
|---|---|---|---|---|---|
| Initial Architecture & Planning | Propose modular monolith with Spring Boot 3.3.4, Java 17, Spring Data JPA, H2/PostgreSQL. | Approved full implementation plan. | ACCEPTED | Keeps architecture clean, maintainable, and aligned with standard Spring Boot best practices. | Written plan in `implementation_plan.md` reviewed and approved by engineer. |
| Click Count Concurrency | Use atomic database update query (`UPDATE short_url SET click_count = click_count + 1 WHERE short_code = :code`) rather than JPA entity save. | Approved atomic query approach. | ACCEPTED | Prevents lost updates under concurrent redirect requests. | Verified with `RedirectControllerIntegrationTest` asserting `clickCount` mutation. |
| Expiration Policy for Analytics | Return HTTP 200 OK with metrics for expired short URLs on `/api/urls/{shortCode}/analytics`. | Approved HTTP 200 OK response for expired analytics. | ACCEPTED | Allows analytics tracking and auditing even after short URLs expire. | Tested via unit and integration tests. |
| Collision Retry Mechanism | Implement retry loop up to 5 attempts using `SecureRandom` Base62 generator, fallback to `CollisionExhaustionException`. | Approved max retries = 5. | ACCEPTED | Prevents infinite loop while guaranteeing uniqueness against DB constraint. | Unit test `createShortUrl_collisionRetrySuccess` and `createShortUrl_collisionExhaustion` verified with Mockito. |
| URL Validation | Enforce strict `http` and `https` schemes with max length 2048 chars via `UrlValidatorUtil`. | Approved URL validation rules. | ACCEPTED | Prevents Open Redirect and SSRF vulnerabilities from invalid schemes (e.g. `javascript:`, `ftp:`). | Tested in `UrlValidatorUtilTest` and `UrlControllerIntegrationTest`. |
| Project Build & Integration Test Execution | Run full Maven build `mvn clean test` using Apache Maven 3.9.9 and OpenJDK 17. | Run build and execute tests. | ACCEPTED | Ensures 100% test pass rate and clean compilation. | Executed `mvn clean test`: 26 tests passed, 0 failures, 0 errors. |

## Iteration 2: Engineer Verification & Decision Review

| Task | AI Recommendation | Engineer Decision | Status | Rationale | Validation Performed |
|---|---|---|---|---|---|
| Collision Transaction Isolation | Isolate `saveAndFlush` into `ShortUrlSaveHelper` with `Propagation.REQUIRES_NEW`. | Fix transaction poisoning on duplicate key errors while keeping 5 retries max. | ACCEPTED | Prevents database constraint failure on attempt 1 from marking outer transaction `rollbackOnly`. | Created `CollisionRetryIntegrationTest` verifying DB collision retry success. |
| Generic 500 Information Leakage | Return fixed client-safe message `"An unexpected internal error occurred."` and log stack trace with Slf4j. | Sanitize generic 500 response error text. | ACCEPTED | Prevents leaking raw internal exception strings or DB details to callers. | Verified with `GlobalExceptionHandler` logs and integration tests. |
| CRLF / Control Character Validation | Reject URLs containing `\r`, `\n`, `\t` in `UrlValidatorUtil`. | Enforce control character rejection. | ACCEPTED | Blocks HTTP response splitting and header injection attacks. | Added unit & integration tests (`createShortUrl_crlfInjection_returns400BadRequest`). |
| URL Length Boundary Tests | Add explicit tests for 2048 char acceptance and 2049 char rejection. | Verify 2048 boundary behavior. | ACCEPTED | Validates strict boundary adherence. | Added boundary tests in `UrlValidatorUtilTest` & `UrlControllerIntegrationTest`. |
| Rate Limiting Infrastructure | Add application-level rate limiting library (Bucket4j/Redis). | Defer rate-limiting dependency to production gateway. | DEFERRED | Ingress proxies (Nginx/Gateway) handle rate limiting cleanly without adding stateful app libraries. | Documented in `README.md`, `ARCHITECTURE.md`, `DECISIONS.md`. |
| SSRF & Internal Address Blacklisting | Add IP address blacklist for `127.0.0.1` and cloud metadata IPs. | Defer IP blacklisting to production network policy. | DEFERRED | Service issues 302 client redirects without backend fetching; true server SSRF does not occur. | Documented in `README.md`, `ARCHITECTURE.md`, `DECISIONS.md`. |
## Iteration 3: Brownfield Custom Short Alias Feature

| Task | AI Recommendation | Engineer Decision | Status | Rationale | Validation Performed |
|---|---|---|---|---|---|
| Database Schema Strategy | Expand single `short_code` column length to 64 vs dual-column design. | Accepted single-column design; set `short_code` length to 30. | ACCEPTED WITH MODIFICATION | Avoids dual index overhead; engineer modified max length to 30 (from AI's 64) for cleaner database bounds. | Updated `ShortUrl.java` `@Column(length = 30)`. Verified with H2 JPA schema update. |
| Custom Alias Validation Constraints | Allow length 3–32 chars, regex `^[a-zA-Z0-9_-]+$`. | Modified length to 4–30 chars; lowercase `a-z0-9_-` only; reject leading/trailing `-` or `_`. | ACCEPTED WITH MODIFICATION | Length 4–30 prevents short namespace pollution and keeps aliases concise. Case normalization prevents duplicate confusion. | Added unit tests in `UrlValidatorUtilTest` & integration tests in `CustomAliasIntegrationTest`. |
| Reserved Keyword Protection | Block system endpoints (`api`, `swagger-ui`, `v3`, `h2-console`, `actuator`, `error`, etc.). | Approved exact list of reserved keywords returning HTTP 400 Bad Request. | ACCEPTED | Prevents custom aliases from shadowing or hijacking core application routes. | Tested in `CustomAliasIntegrationTest` asserting `400 Bad Request`. |
| Custom Alias Collision Behavior | Return HTTP 409 Conflict without auto-retrying random codes. | Approved HTTP 409 Conflict; no automatic retries for custom aliases. | ACCEPTED | Retrying with a random code when a custom alias is requested violates user intent. | Tested in `CustomAliasIntegrationTest` asserting `409 Conflict`. |
## Iteration 4: Ambiguous Requirement Resolution – URL Expiration

| Task | AI Recommendation | Engineer Decision | Status | Rationale | Validation Performed |
|---|---|---|---|---|---|
| Expiration Optionality & Default TTL | Support optional expiration (`expiresAt == null` $\rightarrow$ infinite lifetime); no default TTL. | Accepted optional expiration with no default TTL. | ACCEPTED | Preserves backward compatibility for legacy callers and supports permanent links. | Verified with `UrlValidatorUtilTest` & `UrlServiceImplTest`. |
| Input Format Semantics | Support both absolute `expiresAt` timestamp and relative `ttlSeconds`. | Reject `ttlSeconds`; support only `expiresAt` ISO-8601 UTC timestamp. | REJECTED | Avoids unnecessary API complexity, mutual-exclusivity validation, and precedence rules in prototype. | Verified `CreateUrlRequest` supports `expiresAt` (`Instant`). |
| Expired Redirect Response | Return HTTP 410 Gone on expired link redirect. | Accepted HTTP 410 Gone; do not return 404 or HTML landing page. | ACCEPTED | Correct REST HTTP semantics (resource existed but is no longer available). | Verified with `UrlControllerIntegrationTest` & `UrlServiceImplTest`. |
| Click Count Behavior | Do not increment `clickCount` when redirecting an expired URL. | Accepted; perform expiration check prior to atomic click count increment. | ACCEPTED | Accurately reflects successful redirects vs failed expired access attempts. | Verified with Mockito `never().incrementClickCount(...)` in unit tests. |
| Analytics Access Post-Expiration | Return HTTP 200 OK with analytics for expired URLs. | Accepted HTTP 200 OK for expired analytics. | ACCEPTED | Preserves historical auditability and metrics for post-campaign analysis. | Verified in `UrlServiceImplTest` and `CustomAliasIntegrationTest`. |
| Database Retention & Cleanup | Add `@Scheduled` background worker to purge expired records from DB. | Defer cleanup; retain expired records in database. | DEFERRED | Deleting records destroys historical analytics and introduces background infrastructure beyond scope. | Documented retention policy in `docs/scenarios/AMBIGUOUS.md`. |
| Short Code / Alias Recycling | Prohibit immediate recycling of expired codes/aliases. | Accepted; UNIQUE DB constraint remains authoritative. | ACCEPTED | Prevents traffic hijacking and phishing risks associated with recycled custom aliases. | DB unique constraint enforced via JPA `@Column(unique = true)`. |
| Post-Creation Modification | Add `PATCH /api/urls/{shortCode}` to extend or remove expiration. | Defer `PATCH` expiration modification. | DEFERRED | Mutation APIs require authorization and auditing outside prototype requirements. | Expiration remains immutable post-creation. |
| Expiration Boundary Evaluation | Refine boundary check from `isBefore(now)` to `expiresAt <= now` (`!expiresAt.isAfter(now)`). | Edited boundary check to `!expiresAt.isAfter(now)`. | EDITED | Ensures a URL is expired at the exact expiration instant; single clock capture per decision. | Updated `UrlServiceImpl.resolveAndRedirect` & `UrlValidatorUtil.validateExpiration`. Unit tests updated. |
| Minimum TTL Constraint | Introduce a minimum 60-second TTL constraint. | Defer minimum TTL constraint. | DEFERRED | Abuse prevention is managed via ingress rate-limiting policy. | Documented in `docs/scenarios/AMBIGUOUS.md`. |

